const upUrl = (url) => {
  //const fullUrl = "https://api.afrapardaz.com/" + url;
  const fullUrl = "http://192.168.2.2:8282/" + url;
  //const fullUrl = "http://localhost:8282/" + url;
  return fullUrl;
};

export default upUrl;
